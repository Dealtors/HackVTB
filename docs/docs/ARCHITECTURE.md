# 🏗 Архитектура Оркестр API

## Диаграмма модулей

```mermaid
graph TB
    A[Frontend React] --> B[API Gateway]
    B --> C[Backend Spring Boot]
    C --> D[ML Service Python]
    C --> E[PostgreSQL]
    C --> F[External APIs]
    
    subgraph "Backend Modules"
        C1[BPMN Parser]
        C2[OpenAPI Parser]
        C3[Test Orchestrator]
        C4[Execution Engine]
    end
 ```

## 🤖 Использование ИИ в системе

![Использование ИИ](docs\docs\MLModule.md)


```sequenceDiagram
sequenceDiagram
participant U as User
participant F as Frontend
participant B as Backend
participant M as ML Service
participant E as External API

    U->>F: Нажать "Run Test"
    F->>B: POST /api/execute
    B->>M: Запрос тестовых данных
    M-->>B: Сгенерированные payload-ы
    B->>E: Вызов API 1
    E-->>B: Ответ 1
    B->>E: Вызов API 2
    E-->>B: Ответ 2
    B-->>F: Результаты в реальном времени
    F-->>U: Визуализация прогресса
```

## 🛠 Технологический стек
### Backend (Основной сервер)
#### **Java 17 - основной язык разработки**

* Spring Boot 3.1 - фреймворк для enterprise-приложений

* Spring Web MVC - REST API

* Spring Data JPA - работа с базой данных

* Spring Security - аутентификация и авторизация

* PostgreSQL 14 - реляционная база для структурированных данных

#### **ML Service (Сервис искусственного интеллекта)**

* Python 3.9 - язык для ML-разработки

* FastAPI - высокопроизводительный веб-фреймворк

* ONNX Runtime - движок для выполнения ML-моделей

Л* окальный inference - модели работают без интернета

* Кроссплатформенность - работает на CPU без GPU

* Hugging Face Transformers - библиотека для NLP-моделей

* scikit-learn - классические ML-алгоритмы

### Frontend
* React 18 - пользовательский интерфейс

* TypeScript - типизированный JavaScript

* Redux Toolkit - управление состоянием

* Mermaid.js - отрисовка диаграмм и графиков

### Инфраструктура
* Docker - контейнеризация сервисов

* Maven - сборка Java-проекта

* npm - управление фронтенд-зависимостями

## 💾 Хранилища данных

Ссылка на ERP-диаграмму: 

![ERP-диаграмма](docs\docs\DATABASE.md)